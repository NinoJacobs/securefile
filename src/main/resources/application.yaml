spring:
  application:
    name: securefile
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  lifecycle:
    timeout-per-shutdown-phase: 20s

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

securefile:
  jwt:
    secret: ${SECUREFILE_JWT_SECRET}
    ttl-minutes: ${SECUREFILE_JWT_TTL_MINUTES:5}
    refresh-secret: ${SECUREFILE_JWT_REFRESH_SECRET}
    refresh-ttl-minutes: ${SECUREFILE_JWT_REFRESH_TTL_MINUTES:10080}
    issuer: ${SECUREFILE_JWT_ISSUER}
    audience: ${SECUREFILE_JWT_AUDIENCE}
  download-link:
    secret: ${SECUREFILE_DOWNLOAD_LINK_SECRET}
  s3:
    endpoint: ${SECUREFILE_S3_ENDPOINT}
    region: ${SECUREFILE_S3_REGION}
    bucket: ${SECUREFILE_S3_BUCKET}
    access-key: ${SECUREFILE_S3_ACCESS_KEY}
    secret-key: ${SECUREFILE_S3_SECRET_KEY}
    path-style-access-enabled: ${SECUREFILE_S3_PATH_STYLE_ACCESS_ENABLED:true}
